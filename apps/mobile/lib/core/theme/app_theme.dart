// Theme tokens, ThemeData, and shared presentational widgets moved to
// packages/takwa_ui — this file re-exports them so every existing
// `import 'core/theme/app_theme.dart'` across the app keeps working
// unchanged. See the audit's "Architecture & Code Quality" section.
export 'package:takwa_ui/takwa_ui.dart';
