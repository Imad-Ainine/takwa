name: takwa_core
description: >
  Shared Islamic-productivity domain logic for Takwa — enums and the Taqwa
  points model — independent of Flutter, Drift, or any storage layer, so
  it can be used from the mobile app, a future web companion, or tested
  in isolation.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.4

dependencies:

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
