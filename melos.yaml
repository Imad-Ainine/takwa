name: takwa
packages:
  - apps/mobile
  - apps/web
  - packages/*

command:
  bootstrap:
    usePubspecOverrides: true

scripts:
  test:
    run: dart run melos exec --dir-exists=test -- flutter test
    description: Run tests across packages with a test directory.
